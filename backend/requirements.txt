--extra-index-url https://download.pytorch.org/whl/cpu

# Core web framework
fastapi==0.109.2
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Cache / Broker
redis==5.0.4
celery==5.4.0

# AI / ML
torch==2.1.0+cpu
openai==1.14.2
sentence-transformers==3.0.0
spacy==3.7.4
scikit-learn==1.5.0
numpy==1.26.4

# Vector DB
pinecone-client==4.1.0

# Auth / Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
cryptography==42.0.5

# Config / Validation
pydantic==2.7.1
pydantic-settings==2.2.1

# PDF Generation
reportlab==4.2.0

# Rate Limiting
slowapi==0.1.9

# HTTP client
httpx==0.27.0

# Utilities
python-dotenv==1.0.1

# ── Test dependencies (not needed in production) ──────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0              # already listed above, version pinned here for clarity
aiosqlite==0.20.0          # SQLite async driver for in-memory test DB
fakeredis[aioredis]==2.23.2  # in-process Redis fake — no server required
